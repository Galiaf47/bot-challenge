// @flow

const settings = {
  windowSize: 600,
  fieldSize: 5000,
  fps: 60,
  roundTime: 60, // sec
  simulationChunkSize: 100,
  snackMass: 100,
  initialCellMass: 500,
  minSplitMass: 700,
  chargeTime: 20, // frames
  splitTime: 500, // frames
  maxSplitCells: 4,
};

export default settings;
