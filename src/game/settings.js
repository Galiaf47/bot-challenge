// @flow

const settings = {
  playerSize: 32,
  windowSize: 600,
  fieldSize: 5000,
  borderSize: 10,
  ballFriction: 0.95,
  fps: 20,
  roundTime: 60 * 5, // sec
  simulationChunkSize: 100,
};

export default settings;
